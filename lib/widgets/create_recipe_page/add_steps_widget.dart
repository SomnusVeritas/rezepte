import 'package:flutter/material.dart';

class AddCookingStepsWidget extends StatelessWidget {
  const AddCookingStepsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
